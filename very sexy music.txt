-- RED ME --
Since roblox audio sucks replace those id with yours

local sound = Instance.new("Sound")
sound.SoundId = "rbxassetid://10986659478" -- U can replace with the ur id
sound.Looped = true
sound.Parent = workspace
sound:Pause()
        end
    end)
end)
local nh = function()
sound:Play()
end
game.Players.PlayerAdded:Connect(function(player) -- runs every time a player joins
    player.Chatted:Connect(function(msg) -- runs once the player that joined chats a message
        if msg == ":mosic power trup" then pcall nh
            -- do stuff here if the condition is met
print("sexy skid")
game.Players.PlayerAdded:Connect(function(player) -- runs every time a player joins
    player.Chatted:Connect(function(msg) -- runs once the player that joined chats a message
        if msg == ":mosic" then pcall stap
            -- do stuff here if the condition is met
local stap = function()
sound:Stop()
Cheektop:Stop()
end
